---
to: src/containers/<%= name %>/<%= name %>-container.tsx
---
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'
import {
  Text,
  View,
} from 'react-native'

const <%= h.changeCase.pascal(name) %>Container = () => {

  return (
    <SafeAreaView
    testID="<%= h.changeCase.pascal(name) %>TestID"
    >
    <Text>
      My container
    </Text>
    </SafeAreaView>
  )
}

export default <%= h.changeCase.pascal(name) %>Container
