---
to: src/components/<%= h.changeCase.lower(path) %>/<%= h.changeCase.lower(name) %>.tsx
---
import React from 'react'
import { Text, View } from 'react-native'

interface Props {}

const <%= h.changeCase.pascal(name) %>: React.FC<Props> = ({}) => {
  return (
    <View
      testID="<%= h.changeCase.pascal(name) %>TestID"
    >
      <Text >
        My component
      </Text>
    </View>
  )
}

<%= h.changeCase.pascal(name) %>.defaultProps = {}

export default <%= h.changeCase.pascal(name) %>